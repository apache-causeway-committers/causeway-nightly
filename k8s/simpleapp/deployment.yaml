apiVersion: apps/v1
kind: Deployment
metadata:
  name: isis-app-simpleapp
spec:
  selector:
    matchLabels:
      app: isis-app-simpleapp
  replicas: 1
  template:
    metadata:
      labels:
        app: isis-app-simpleapp
    spec:
      containers:
        - image: apacheisis/demo-wicket:latest
          imagePullPolicy: Always
          name: isis-app-simpleapp
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: isis-app-simpleapp
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: isis-app-simpleapp
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: isis-app-simpleapp
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
    - hosts:
        - demo-wicket.isis.incode.work
      secretName: isis-app-simpleapp-tls
  rules:
    - host: simpleapp.isis.incode.work
      http:
        paths:
          - path: /
            backend:
              serviceName: isis-app-simpleapp
              servicePort: 80

