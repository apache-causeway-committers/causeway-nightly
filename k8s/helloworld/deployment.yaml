apiVersion: apps/v1
kind: Deployment
metadata:
  name: isis-app-helloworld
spec:
  selector:
    matchLabels:
      app: isis-app-helloworld
  replicas: 1
  template:
    metadata:
      labels:
        app: isis-app-helloworld
    spec:
      containers:
        - image: apacheisis/helloworld:latest
          imagePullPolicy: Always
          name: isis-app-helloworld
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: isis-app-helloworld
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: isis-app-helloworld
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: isis-app-helloworld
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
    - hosts:
        - helloworld.isis.incode.work
      secretName: isis-app-helloworld-tls
  rules:
    - host: helloworld.isis.incode.work
      http:
        paths:
          - path: /
            backend:
              serviceName: isis-app-helloworld
              servicePort: 80

